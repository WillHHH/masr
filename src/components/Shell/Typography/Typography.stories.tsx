import React from "react";
import Typography from "./Typography";

export default {
  title: "Style Guide/Typography",
  component: Typography,
};

const Template = (args) => <Typography {...args} />;

export const TypeStyles = Template.bind({});
TypeStyles.args = {};
